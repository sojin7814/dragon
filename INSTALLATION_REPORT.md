# 외부 캘린더 제거 · 설치 개선

2026-09-20. 기존 드래곤 휴무 앱을 수정했습니다.

## 변경 내용

- 외부 캘린더 배너·팝업·다운로드·공유·도움말, ICS 생성 모듈과 전용 테스트·라이브러리를 제거했습니다. 내부 월간 달력·양도·교환·메모·휴무 계획·비교 달력·백업은 유지합니다.
- 브라우저의 `beforeinstallprompt` 이벤트를 React 렌더 전에 수신합니다. 보관한 이벤트가 있을 때만 아이콘이 있는 설치 카드와 ‘홈 화면에 설치’ 버튼을 제공합니다. 실제 사용자 클릭 안에서 `prompt()`를 호출합니다.
- 이벤트가 없을 때 작동하지 않는 버튼을 표시하지 않습니다. 설정에는 준비 상태를 짧게 안내합니다. 취소·실패한 이벤트는 재사용하지 않으며 새 이벤트가 오면 다시 설치할 수 있습니다.
- 설치 수락과 설치 완료를 구분합니다. `appinstalled` 또는 standalone/fullscreen 실행 상태에서는 설치 카드·버튼을 숨깁니다. 설치 완료 여부를 영구적인 로컬 플래그로 추측하지 않아 삭제 후 다시 설치할 수 있습니다.
- 상단 카드의 닫기는 한국 날짜 기준 당일 유지됩니다. 설정의 준비된 설치 버튼은 계속 사용할 수 있습니다. 설치 확인 중에는 서비스워커의 안전 업데이트가 기다립니다.
- 카카오톡·인앱 WebView에서는 설치를 강제로 요청하지 않습니다. 짧은 외부 브라우저 안내와 실제 주소 복사를 제공합니다. 보장할 수 없는 자동 브라우저 전환 링크는 추가하지 않았습니다.
- QR은 앱 주소만 담습니다. 앱 공유 화면·A4 안내·카카오톡 홍보물은 ‘QR 촬영 → 앱 열기 → 홈 화면에 설치 → 설치’ 흐름을 안내합니다.

## 저장 데이터와 PWA

`dragon_calendar_data`, 데이터 schemaVersion 1, 백업 formatVersion 1과 모든 레코드 구조를 유지했습니다. 설치 처리는 사용자 기록을 쓰지 않습니다. 별도 키 `dragon_calendar_data_install_dismissed_on`에는 안내를 닫은 날짜만 저장합니다.

주소·manifest id·start_url·scope는 `/dragon/`이며 display는 `standalone`입니다. 기존 name/short_name ‘드래곤 휴무’, 테마·배경색, 192/512/maskable/Apple touch/favicon을 유지합니다. 최종 아이콘은 변경하지 않았습니다.

같은 Android 브라우저와 동일 출처의 설치 앱은 기존 사이트 저장소를 사용합니다. 다른 브라우저로 옮기는 것은 별도 저장소이므로 기존 백업·복원 기능을 이용합니다. Android 실제 설치 전후 저장소 확인은 아래와 같이 미검증입니다.

기존 서비스워커의 오프라인·입력 보호·다중 창 확인·실패 시 이전 버전 유지 동작을 보존했습니다. 업데이트를 위해 사이트 데이터 삭제·쿠키 삭제·재설치를 요청하지 않습니다.

## 검증 결과

단위 테스트 74개, 브라우저 시나리오 35개, TypeScript와 프로덕션 빌드가 통과했습니다. 브라우저 시나리오는 기존 데이터·기능, 설치 이벤트의 수신/클릭/취소/실패/완료, 모의 standalone 기록 유지, 카카오톡 주소 복사, 당일 닫기, 320/390/1280px 라이트·다크 레이아웃, 오프라인과 안전 업데이트를 포함합니다.

QR PNG·SVG·카카오톡 이미지를 다시 생성하고 독립 디코더로 정식 주소 일치를 확인했습니다. 최종 아이콘은 수정하지 않았습니다. Chrome에서 빌드 manifest를 파싱하고 경로·이름·standalone·색·아이콘을 확인했습니다. PC Chrome 자동 브라우저의 설치 가능성 진단에서는 격리된 시크릿 컨텍스트 제한만 보고됐으며, 이를 실제 설치 확인으로 간주하지 않았습니다.

## 공개 배포 확인

[GitHub Actions #4](https://github.com/sojin7814/dragon/actions/runs/35462358022)가 성공했습니다. 코드 커밋과 공개 빌드는 `927a57a1c1049eedd493dcb60e6a68510728ed38`입니다.

2026-09-20 03:49 한국시간에 앱·JS·CSS·manifest·서비스워커·아이콘 5종의 HTTP 200 응답을 확인했습니다. `/dragon/` 하위 경로가 일치하며 공개 아이콘의 바이트와 크기는 기존 최종 아이콘과 같습니다.

기존에 열려 있던 공개 앱이 재설치·사이트 데이터 삭제 없이 새 버전으로 업데이트됐습니다. 외부 캘린더 배너와 버튼이 사라졌으며 하우스 A, 9월 휴무 7일, 다크 테마, 내부 달력·휴무 계획·푸터를 유지했습니다. 설정에는 설치 준비 상태가 표시됐고 작동하지 않는 설치 버튼은 없었습니다. 브라우저 오류 로그는 없었으며 공개 사용자 기록을 테스트 데이터로 대체하지 않았습니다.

실제 Android Chrome·Samsung Internet의 설치 확인창, 홈 화면 아이콘 생성·실행, 설치 전후 기록, 실기기 업데이트 및 카카오톡 외부 브라우저 전환은 **미검증**입니다. PC Chrome에서 이벤트를 모의하고 모바일 화면 폭·사용자 에이전트·display mode를 바꾼 시험은 실기기 완료로 간주하지 않습니다.

## 구현 근거

- [MDN beforeinstallprompt](https://developer.mozilla.org/en-US/docs/Web/API/Window/beforeinstallprompt_event): 이벤트 수신 후 버튼 표시 및 사용자 클릭에서 prompt 호출. 이벤트 발생 시점은 보장되지 않습니다.
- [Chrome/web.dev 설치 UI](https://web.dev/articles/customize-install): 취소·설치 수락·appinstalled 처리와 설치 후 홍보 UI 숨김.
- [실기기 확인표](docs/device-checks.md)

이전 IMPLEMENTATION_REPORT.md와 REVISION_REPORT.md는 과거 배포 기록입니다. 과거 문서의 ICS 관련 설명은 현재 기능을 의미하지 않습니다.
