# 드래곤 휴무 구현·검증 결과

> 2026-09-20 추가 수정본의 변경사항과 데이터 호환·최신 검사 결과는 [추가 수정 보고서](REVISION_REPORT.md)를 참고하세요. 아래 내용은 최초 구현·배포 당시의 기록입니다.

갱신일: 2026-09-20. 앱 구현·자동 검증을 마치고 사용자가 지정한 **[sojin7814/dragon](https://github.com/sojin7814/dragon)** 공개 저장소에서 **[드래곤 휴무](https://sojin7814.github.io/dragon/)**를 배포했습니다. **2026-09-20 00:10 한국시간에 공개 주소의 HTTP 200 응답과 실제 브라우저 첫 화면을 확인했습니다.** 전달받은 PNG를 최종 아이콘으로 적용했습니다. 휴대폰 OS 설치·실제 캘린더 가져오기·QR 카메라 촬영은 미검증입니다.

## 지금 실행하기

이 폴더에서 `pnpm dev`를 실행하면 개발 화면을, `pnpm build` 후 `pnpm preview`를 실행하면 설치·오프라인을 포함한 배포 파일을 확인할 수 있습니다. 이 작업에서는 `http://127.0.0.1:4173/dragon/`으로 빌드 미리보기를 실행했습니다. 로컬 주소는 이 PC 전용입니다.

상세 명령과 설명: [README](README.md). 최초 공개 확인한 빌드: `e7174af093734e57b8df8e863f00f816ffc3ab49`, 패키지 버전 `0.1.0`. 이후 문서나 코드가 `main`에 반영되면 빌드 식별자는 바뀔 수 있습니다.

## 확정 사항과 범위

- A 첫 휴무 기준일은 `2025-12-02`. 사용자가 **2026-09-19에 현재도 같은 순환임을 확인**했습니다.
- A/B/C/D 범위는 제공된 문구 그대로 사용합니다. 실제 1~7조 번호로 자동 분류하지 않습니다.
- 양도 관행은 추측하지 않고 실제 근무/휴무를 직접 선택하도록 구현했습니다. 대바는 근무입니다.
- Vite + TypeScript + React로 만든 정적 PWA입니다. 로그인, Firebase, 서버 데이터베이스, 외부 추적, 연락처 조회가 없습니다.
- 데이터가 작고 두 날짜 교환을 한 번의 원자적 쓰기로 저장하기 위해 **localStorage 단일 레코드**를 사용했습니다. 수년치 계산 일정을 저장하지 않습니다.
- 저장 키는 `dragon_calendar_data`, 복원 전 안전 복사 키는 `dragon_calendar_data_before_restore`입니다. 앱 빌드 버전과 무관하게 유지합니다.
- 별도 `dragon-calendar/` 폴더와 로컬 Git 저장소를 만들었습니다. 기존 두 구현 문서, 수입장부, AION2 프로젝트는 수정하지 않았습니다. 기존 원격 저장소의 최초 커밋 `523daad`를 유지하면서 승인된 소스를 반영했고, GitHub Actions 배포와 공개 접속을 확인했습니다. 이번 보고서 갱신 전 원격 56개 파일과 로컬 내용이 일치하고 `git diff origin/main`이 비어 있는 것을 확인했습니다.

## 요구사항별 구현 위치

| 요구사항 | 구현 파일 | 검증과 상태 |
|---|---|---|
| 개인용 독립 앱, 기존 앱 보존 | `src/config.ts`, `src/storage.ts`, `assets/service-worker.js` | 보호 키 보존 테스트, 전역 저장소 초기화·다른 서비스워커 해제 없음 |
| A/B/C/D 선택·범위·다음 휴무 확인 | `src/App.tsx`, `src/config.ts`, `src/domain.ts` | 선택 확인 2단계, 현재 휴무 기간 표시, 화면 테스트 통과 |
| 2일 휴무 + 6일 기본 근무 | `src/domain.ts`, `tests/domain.test.ts` | UTC 날짜 정수, 한국 기준 오늘, 5개월 캡처 기대값 대조 통과 |
| 월간 달력·연도/월 직접 이동·오늘 | `src/App.tsx`, `src/styles.css` | 4/5/6주, 월말·윤년·연말, 1900~2199년 선택 |
| 휴무 양도·교환·대바·직접 수정·메모 | `src/DateEditor.tsx`, `src/domain.ts` | 원래 일정·실제 상태 분리, 저장 전 확인, HTML 텍스트 처리 |
| 연결된 교환 수정·취소 | `src/domain.ts`, `src/DateEditor.tsx` | 두 날짜 원자 저장, 다른 달 교환, 충돌 차단, 독립 메모 보존 |
| 휴무조 변경 적용일·이력 | `src/Settings.tsx`, `src/domain.ts` | 미래/여러 이력/중복일 검사, 편집·취소, 기존 기록 영향 미리보기 |
| 밝게·어둡게·기기 설정 | `src/App.tsx`, `src/Settings.tsx`, `src/styles.css` | 재실행 유지, 설정·입력창 일관 적용, 320px/390px 화면 확인 |
| 저장 실패·손상 원본·여러 창 보호 | `src/storage.ts`, `src/App.tsx` | 오류 시 입력 보존, 원본 다운로드·복원 경로, Web Locks + 원본 비교 |
| 백업·검사·미리보기·복원 | `src/storage.ts`, `src/Settings.tsx` | 앱/형식/내용 검증 후 대체, 직전 안전 복사본 다시 다운로드 가능 |
| 앱 안 사용설명서·삭제 안내 | `src/Help.tsx`, 초기 화면·설정 | 캐시와 사이트 데이터 구별, 환경 간 이동, 개인정보 보호 모드 설명 |
| 보고 있는 달 캘린더 추가 | `src/ics.ts`, `src/App.tsx`, `tests/ics.test.ts` | .ics 저장/지원 시 공유, 실제 변경 반영, 일정 수·개인정보·중복 안내 |
| 표준 종일 일정 파일 | `src/ics.ts` | CRLF, UTF-8 75옥텟 접기, 특수문자, 다음날 종료, UID 안정성 및 독립 파서 검증 |
| 설치 안내·설치 버튼 상태 | `src/pwa.ts`, `src/Help.tsx` | 이벤트 있을 때만 설치 버튼, 취소/설치 신호 모의 테스트, iPhone 그림 안내 |
| 카톡·iPad·PC 설치 도움말 | `src/Help.tsx`, `src/Share.tsx` | 외부 브라우저 열기/주소 복사 안내, 강제 앱 실행 없음 |
| manifest·오프라인·경로 격리 | `scripts/build-pwa.mjs`, `assets/service-worker.js` | 실제 Chrome 루트 및 `/dragon-test/` 경로에서 오프라인 재실행·저장 통과 |
| 입력 보호·여러 창 안전 업데이트 | `src/pwa.ts`, `assets/service-worker.js` | 모든 창의 안전 상태 확인 2단계, 입력 차단 후 1회 재실행, 숨겨진/응답 없는 창은 적용 보류 |
| 다운로드 실패·기존 캐시 보존 | `assets/service-worker.js`, `tests/pwa*.ts` | 새 버전 다운로드 실패 후 기존 버전 오프라인 실행 통과, 이전 캐시 유지 |
| GitHub Pages 자동 배포 | `.github/workflows/deploy.yml` | 잠금 설치→단위 테스트→QR 자체 검사→타입/빌드→Pages, 실제 Actions #2 성공 및 공개 접속 확인 |
| 정식 URL·QR·주소 공유 | `src/Share.tsx`, `src/files.ts`, `scripts/generate-qr.mjs` | 정식 주소 설정, 미확정/잘못된 URL 차단 유지, 생성 PNG/SVG·카톡 이미지 독립 디코딩 통과 |
| 인쇄/카톡 안내 | `docs/a4-template.html`, `docs/kakao-template.svg`, `docs/kakao-message-template.txt` | `generated/qr/`에 정식 주소의 A4 HTML·PNG·SVG·카톡 PNG/문구 생성 완료, 공개 접속 확인 후 공유·인쇄 가능. 실물 카메라 촬영은 미검증 |
| 최종 원본 아이콘·파생 생성 | `assets/icon-original.png`, `assets/icon-verification.json`, `scripts/generate-icons.mjs` | 원본 SHA256 일치, 무변형 리사이즈로 192/512/iOS180/favicon32, 여백만 추가한 마스크512 생성 |

## 실제 수행한 자동 검증

| 검사 | 결과 | 확인 내용 |
|---|---|---|
| TypeScript 검사·배포 빌드 | 통과 | 오프라인 앱 셸 9개 파일, 외부 CDN 의존 없음 |
| Vitest | **76개 통과** | 도메인·저장·ICS 69개, 서비스워커 7개 |
| 화면 Playwright / PC Chrome | **9개 통과** | 첫 선택, 캡처 달력, 다크/모바일, 대바·메모 취소, 다른 달 교환, 내보내기, 정상/손상 복원, 쓰기 실패, 다른 창 충돌, 원본 보호, 미래 조 이동, 정식 QR 표시, 설치 상태 |
| 초기 생산 빌드 PWA / 루트 | **4개 통과** | 오프라인 재실행·작성, 입력 후 안전 업데이트, 다른 창 입력 보호, 새 파일 다운로드 실패 |
| 생산 빌드 PWA / `/dragon/` 및 `/dragon-test/` | **각 4개 통과** | 하위 경로 새로고침·오프라인·업데이트, manifest/아이콘/서비스워커 범위 일치 |
| QR 자체 검사 | **로컬·Ubuntu CI 통과** | PNG, SVG 래스터, 카톡 이미지의 jsQR 디코딩 주소 일치, 금지 주소 차단, 카톡 QR 영역 픽셀 동일성 검사 |
| 실제 GitHub Actions 배포 | **#2 성공** | Vitest 76개·QR 검사·타입/빌드·Pages 게시 통과. 빌드 49초, 게시 8초, 전체 1분 5초 |
| 공개 주소·정적 파일·아이콘 | **접속·동일성 확인** | 앱·JS·CSS·manifest·서비스워커·아이콘 5종 HTTP 200, 공개 PNG와 로컬 PNG의 바이트·SHA256·크기 일치, 첫 화면·192px 헤더 아이콘 정상, 오류 로그 0개 |
| 화면 육안 확인 | 완료 | 실제 Chrome 렌더링의 PC·390px·다크 스크린샷 확인, 가독성 수정 |

최종 로컬 브라우저 검사는 화면 9개와 `/dragon/` PWA 4개를 합쳐 13개가 통과했습니다. 초기 루트 및 `/dragon-test/` 경로 검사도 표에 별도로 기록했습니다. 화면 검사 중 조 선택 컨트롤의 접근성 이름을 명시했고, 첫 설치 중 업데이트 안내가 보이는 문제와 초기 선택 이후 업데이트 보호가 계속 켜지는 문제를 수정했습니다. 회귀 검사는 수정 후 통과했습니다.

설치 이벤트 테스트는 제어된 모의 이벤트입니다. 실제 OS 설치 완료 시험으로 세지 않습니다. 휴대폰 폭의 Chrome은 iPhone/Safari 또는 삼성 인터넷 실기기 시험이 아닙니다.

Windows 테스트 환경의 자동 서버 종료가 지연되어 별도 미리보기 서버를 실행하고 `SKIP_WEB_SERVER=1`로 화면 검사를 수행했습니다. 검사 프로세스는 정상 종료했습니다. 첫 원격 CI에서는 카톡 안내 이미지의 QR 디코딩이 실패했습니다. SVG 안 이미지의 보간 결과에 의존하지 않도록 정수 배율 PNG를 무손실로 복사하고 QR 영역 픽셀 동일성 검사를 추가했습니다. 수정 후 로컬과 Ubuntu CI에서 독립 디코딩을 포함한 QR 검사가 통과했습니다.

## 공개 배포·QR·아이콘 확인 완료

- **원격 반영과 공개 접속**: [Actions #2](https://github.com/sojin7814/dragon/actions/runs/35450873698)가 성공했습니다. 2026-09-20 00:10 한국시간에 공개 주소를 열어 ‘드래곤 휴무’ 제목과 A/B/C/D 휴무조·다음 휴무 표시를 확인했습니다. manifest의 `id`, `start_url`, `scope`는 모두 `/dragon/`입니다. 이 확인은 실제 휴대폰 OS 설치 검증과 구분합니다.
- **정식 QR 주소 확인**: `.env`와 배포 workflow에 승인 URL을 설정했습니다. PNG/SVG·A4 HTML·카톡 PNG/문구를 `generated/qr/`에 생성했고 독립 디코더 결과는 공개 접속을 확인한 `https://sojin7814.github.io/dragon/`과 일치합니다. 이 파일을 공유하거나 인쇄할 수 있으며 카메라 촬영 시험은 아래 미검증 항목으로 남깁니다.
- **최종 아이콘 적용**: 사용자가 제공한 1254×1254 PNG를 `assets/icon-original.png`에 바이트 그대로 보관했습니다. 512/192/180/32 및 maskable512를 생성했습니다. 일반 아이콘은 원본 전체 축소만, maskable은 원본 축소+가장자리에서 얻은 색의 여백만 사용합니다. 색상/용/골프공/달력 구성 변경이나 AI 재생성은 없습니다. 원본 SHA256은 `4313c632ac5955fc8f3a070586ffaa6a5939b9f0a23900f89f64a009f690c8da`입니다. 공개 아이콘 5종도 로컬 결과와 바이트·SHA256·크기가 일치합니다. OS가 보관한 이전 아이콘의 즉시 교체는 보장하지 않습니다.

## 아직 실기기로 확인해야 하는 것

1. **실제 휴대폰 설치·저장 환경**: 갤럭시 Chrome/삼성 인터넷, iPhone Safari/홈 화면, iPad, 카톡 내부 브라우저에서 확인해야 합니다. PC Chrome/Edge OS 앱 설치도 별도 미검증입니다.
2. **실제 캘린더 등록**: .ics 파일 생성·다운로드·표준 파싱은 확인했습니다. 삼성 캘린더와 iPhone 기본 캘린더의 월 전체 가져오기·반복 가져오기 결과는 미검증입니다. iPhone에서 파일 미리보기만 되는 경우 Mail 첨부 경로 등 사용자 선택 대안을 앱 설명서에 적었습니다. 자동 등록/상시 동기화는 제공하지 않습니다.
3. **QR 촬영 시험**: 종이·PC 화면을 휴대폰 카메라로 촬영하는 시험이 필요합니다. 자동 디코딩 결과로 촬영 시험까지 대체하지 않습니다.

## 데이터·업데이트의 한계

- 기기 저장은 영구 보관이나 서버 백업이 아닙니다. 다른 기기·브라우저와 자동으로 동기화되지 않습니다.
- Web Locks가 없는 브라우저에서는 오래된 원본 비교로 일반적인 덮어쓰기를 감지하지만 서로 다른 탭의 극히 동시적인 쓰기를 엄격한 트랜잭션으로 보장하지 않습니다.
- 현재 데이터 형식은 최초 `schemaVersion: 1`입니다. 같은 형식의 앱 업데이트는 기록을 그대로 읽습니다. 지원하지 않는 형식/손상된 형식을 임의로 바꾸지 않고 원본 보존·복원 안내를 제공합니다.
- 업데이트는 안전 상태를 확인할 수 있는 창들에서 적용합니다. 여러 창·설치창이 열려 있거나 숨겨진 창이 응답하지 않으면 저장 후 다른 창을 닫아야 할 수 있습니다. 모든 사용자에게 동시에 즉시 반영되는 방식이 아닙니다.
- 이전 정상 캐시를 보존하지만 앱 초기화 오류의 모든 원인을 자동 복구하는 것은 아닙니다. 오류 화면은 기록 삭제 없이 재실행·복구를 안내합니다.
- 개인 데이터와 앱 파일은 분리되어 있습니다. 같은 도메인 하위 경로는 저장 공간의 보안 경계가 완전히 분리되는 것은 아닙니다. 캐시·키·서비스워커 범위를 이 앱에 한정합니다.
- 선택 범위는 1900~2199년입니다. 공휴일·회사의 실제 근무 배정·상대방 승인·수입장부 통합은 구현 범위에 포함하지 않았습니다.

## 참고 문서

- [설치·캘린더·QR 실기기 확인표](docs/device-checks.md)
- [최초 GitHub Pages 배포](docs/deploy.md)
- [QR 생성·인쇄 안내](docs/qr.md)
- [아이콘 교체](docs/icons.md)
- [RFC 5545](https://www.rfc-editor.org/rfc/rfc5545)
- [MDN 설치 가능한 PWA](https://developer.mozilla.org/en-US/docs/Web/Progressive_web_apps/Guides/Making_PWAs_installable)
- [서비스워커 수명 주기](https://web.dev/articles/service-worker-lifecycle)

실제 개인 일정·백업·사용자 캡처를 배포 산출물에 넣지 않았습니다. 자동 테스트의 이름·메모는 테스트용 가상 입력입니다.
