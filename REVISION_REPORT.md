# 드래곤 휴무 추가 수정 보고서

2026-09-20. 기존 앱을 수정했으며 새 앱이나 저장소를 만들지 않았습니다.

## 수정 내용

- 하우스 A/B/C/D와 고정 근무반 3종(월~금, 금~일, 토~일)을 지원합니다. 하우스의 기준일과 8일 순환은 그대로이며 고정 근무반은 요일로 계산합니다.
- 최초 선택을 ‘하우스 캐디 / 고정 근무반’으로 나눴습니다. 설정의 현재 유형·적용일 변경·여러 변경 이력·ICS 설명도 7종을 지원합니다.
- 상단 달/해 버튼으로 즉시 테마를 바꾸고 저장합니다. 설정의 밝게·어둡게·기기 설정과 같은 테마를 사용합니다.
- 월간 기록 카드와 메인 유형 변경 선택기를 제거했습니다. ‘다른 근무조 보기’는 내 실제 휴무를 녹색으로 고정하고 여러 유형의 기본 휴무를 이름과 서로 다른 색으로 비교합니다. 비교 선택은 저장하지 않습니다.
- 일정 입력에서 날짜를 바꾸면 그 날짜의 기본 일정·실제 상태·기존 기록을 불러옵니다. + 입력은 보고 있는 달과 관계없이 오늘로 시작합니다. 저장하지 않은 입력은 확인 없이 버리지 않습니다.
- 다가오는 휴무 카드에서 날짜별 계획을 작성·수정·삭제합니다. 달력에는 한 줄로 표시하고 날짜를 열면 전체 내용을 읽을 수 있습니다.
- 새 기록 선택지는 메모만·휴무 양도·휴무 교환입니다. 별도 대바/직접 변경 버튼은 제거하되 ‘그날 나는’의 상태와 상대방을 기록할 수 있습니다. 기존 대바의 이름·메모·사유를 계속 읽고 수정할 수 있습니다.
- 실제 휴무는 녹색, 실제 근무는 핑크입니다. 사유는 텍스트로 유지하며 좁은 화면에서는 사유와 상태를 두 줄로 표시합니다.
- 요청한 황소진 캐디 안내 문구를 메인 하단의 11px 푸터로 추가했습니다. 일반 문서 흐름으로 배치해 버튼을 덮지 않고 화면 폭에 따라 줄바꿈합니다. 밝은/어두운 화면에 각각 읽을 수 있는 차분한 색을 적용했습니다.

## 기존 데이터 호환 방식

- 저장 키 `dragon_calendar_data`, `schemaVersion: 1`, 백업 `formatVersion: 1`을 유지했습니다. 삭제·초기화·일괄 변환을 하지 않습니다.
- 기존 `A/B/C/D` 내부 값과 변경 이력은 그대로 읽고 표시명만 ‘하우스 A/B/C/D’로 매핑합니다.
- 새 값은 `WEEKDAY_MON_FRI`, `WEEKEND_FRI_SUN`, `WEEKEND_SAT_SUN`으로 구분합니다. 기존 구조에서 허용하는 유형을 확장했으므로 파괴적인 마이그레이션이 필요하지 않습니다.
- 휴무 계획은 기존 날짜별 `notes`에 저장합니다. 메모와 별개의 사본을 만들지 않아 기존 백업·복원·ICS와 함께 동작합니다. 기존 `changes`의 대바·양도·교환과 상대방 메모도 유지합니다.
- 기존 백업을 정상 복원할 수 있습니다. 새 근무 유형을 포함한 백업은 업데이트된 앱에서 사용해야 합니다. 예전 앱이 새 유형을 읽지 못하더라도 원본을 임의로 초기화하지 않습니다.
- 비교 화면은 읽기 전용이며 기록 저장 함수를 호출하지 않습니다. 실제 유형 변경은 설정에서만 적용일과 함께 저장합니다.

## 확인한 항목

| 검사 | 결과 |
|---|---|
| 단위 테스트 | 84개 통과: 기존 순환·월 경계·교환·보호 저장·ICS와 새 7종/이전 백업/혼합 이력 |
| 브라우저 검증 | 23개 시나리오 통과: 기존 13개와 추가 10개 |
| 이전 데이터 | A/B/C/D 각각 이전 형식·이력·대바·메모가 로딩만으로 바뀌지 않음, 기존 대바 편집 후 내용 유지 |
| 고정 근무반 | 세 유형 각각 실제 최초 선택과 월간 휴무·근무 표시 확인 |
| 날짜·휴무 계획 | 날짜 변경/입력 보호, 두 날짜 별도 저장, 달력 표시, 전체 내용 조회, 수정·삭제·백업 파일 복원 |
| 비교 | 내 일정 고정, 개인 변경 반영, 여러 유형 선택·해제, 닫기 전후 저장 데이터 동일 |
| 유형 변경 | 하우스→주중반→하우스, 적용일 이전 규칙 유지, 기존 계획 유지 |
| 표시·테마·푸터 | 1280/390/320px, 라이트/다크, 가로 넘침 없음, 푸터와 버튼 겹침 없음, 첫 화면 테마 저장 및 기기 설정 연동 |
| PWA | `/dragon/` 실제 빌드 오프라인 재실행·저장, 입력 보호 업데이트, 여러 창 안전 적용, 실패 업데이트의 이전 캐시 유지 |
| TypeScript·빌드 | 통과, 기존 아이콘·manifest·service worker 하위 경로 유지 |

자동 검사는 격리된 테스트 브라우저와 가상 기록으로 실행했습니다. 공개 앱의 실제 사용자 기록을 초기화하거나 테스트 데이터로 대체하지 않았습니다.

## 배포 상태

사용자가 첨부 문서의 전체 수정사항과 푸터의 공개 배포를 확인했으며, 기존 `sojin7814/dragon` 이력을 유지해 반영했습니다. [Actions #3](https://github.com/sojin7814/dragon/actions/runs/35453207590)에서 단위 테스트 84개·QR 검사·타입/빌드·Pages 게시가 성공했습니다(전체 34초).

2026-09-20 00:54 한국시간에 [공개 앱](https://sojin7814.github.io/dragon/)의 빌드 `96a32553981b99f12f88cfee3cebf6568be824f8`을 확인했습니다. 앱·JS·CSS·manifest·서비스워커·아이콘 5종이 HTTP 200이며 manifest 범위는 `/dragon/`입니다. 아이콘은 이전 최종 원본 기반 파일과 바이트·크기가 일치합니다.

기존 공개 브라우저의 A 설정이 업데이트 후 ‘하우스 A’로 표시되고, 같은 9월 휴무 7일을 유지하는 것을 확인했습니다. 새 비교 버튼·휴무 계획 카드·푸터가 보이고 월간 기록 카드는 사라졌으며 브라우저 오류 로그는 없었습니다. 공개 앱의 사용자 설정이나 기록을 테스트 데이터로 바꾸지 않았습니다.

## 실기기 확인이 필요한 항목

갤럭시/삼성 인터넷·iPhone/iPad 홈 화면 설치, OS별 실제 캘린더 가져오기, 종이 QR 카메라 촬영은 직접 시험하지 않았습니다. 모바일 폭의 PC Chrome 검사와 구분합니다. 개인 기록은 기기 저장이므로 기기 변경 전 백업해야 합니다.
