# 음악 플레이어 — 2026-09-20

기존 달력 아래 빈 공간에 작은 플레이어를 추가했습니다. PC는 오른쪽 카드 아래, 모바일은 달력과 휴무 카드 아래에 표시합니다. 페이지/PWA를 열 때 자동 재생하지 않습니다. 사용자가 재생한 뒤에는 다음 곡으로 이어지고 마지막 곡에서 멈춥니다.

## 들어간 음악

사용자가 제공한 MP3 원본을 변환 없이 사용합니다.

| 곡 | 파일 이름 | 길이 |
|---|---|---|
| 캐디의 하루 | `caddie-day.mp3` / `caddie-day.lrc` | 4:01.44 |
| 오늘 왜 이러지 | `golfer-day.mp3` / `golfer-day.lrc` | 2:56.40 |
| 드래곤, 마음이 쉬어 가는 곳 | `dragon-rest.mp3` / `dragon-rest.lrc` | 3:48.00 |

‘캐디의 하루’의 제공 가사는 6:36.60까지여서 사용자 요청에 따라 현재 음원에 맞춰 시간을 수정했습니다. 로컬 faster-whisper large-v3의 단어 시간과 제공 가사를 순서대로 대조했고, 오인식한 첫 단어와 외침 구간 6곳은 해당 음성 구간의 시작 시간으로 보정했습니다. **130줄 문장은 모두 그대로이며 마지막 줄은 3:59.18입니다.** 단순 시간 비율 축소를 사용하지 않았습니다. 자동 음성 분석에 기초한 정렬이며, 사람이 전곡을 청취해 프레임 단위로 검수한 결과는 아닙니다. 다른 두 곡은 제공한 타임코드를 유지했습니다.

## 새 노래를 넣는 방법

1. `public/assets/music/`에 MP3와 같은 이름의 LRC를 넣습니다. 예: `new-song.mp3`, `new-song.lrc`.
2. `src/tracks.ts`의 `TRACKS` 목록에 아래 항목을 추가합니다. `id`는 다른 곡과 겹치지 않게 정하세요.
3. `pnpm test`와 `pnpm build`로 확인하고 저장소에 반영합니다. 기존 GitHub Pages 배포가 적용하면 목록에 나타납니다.

```ts
{ id: 'new-song', title: '새 노래', audio: 'assets/music/new-song.mp3', lyrics: 'assets/music/new-song.lrc' },
```

`duration: 180`처럼 초 단위 길이를 선택적으로 넣으면 재생 전에도 전체 시간을 표시합니다. 생략하면 최초 재생 시 음원에서 읽습니다. 파일 이름에는 영문·숫자·하이픈 사용을 권장합니다. 경로 앞에 `/`를 붙이지 마세요. 앱이 `/dragon/`를 자동으로 붙입니다.

## 가사와 파일 구조

- `src/MusicPlayer.tsx`: 재생·일시정지·곡 선택·탐색·전체 가사 화면.
- `src/tracks.ts`: 곡 목록 및 마지막 곡 설정. 저장 키는 `dragon_music_player_state`입니다.
- `src/lyrics.ts`: LRC 해석과 현재 줄 찾기.
- `public/assets/music/`: 실제 MP3/LRC 폴더. 공개 시 `/dragon/assets/music/`에 배치됩니다.

`[mm:ss.xx]`, `[mm:ss]`, 소수 1~3자리, 여러 타임코드가 붙은 줄, 같은 시간의 여러 줄을 지원합니다. BOM, CRLF/LF/CR, 빈 줄을 처리하며 제목·가수 등 메타 태그와 시간 없는 줄은 무시합니다. 같은 시간의 가사는 함께 표시하고, 시간이 있는 빈 줄은 앞 가사 표시를 끝냅니다. 1MB가 넘는 LRC는 플레이어 안에서 오류 처리합니다.

가사가 없어도 음악은 재생됩니다. 음원이 없으면 해당 곡에만 안내하고 다른 곡을 선택할 수 있습니다. 기기의 기본 볼륨을 사용합니다. 전곡 오프라인 다운로드 기능은 없습니다.

## 기록과 업데이트

`dragon_calendar_data`, 백업 형식, 아이콘, 설치 설정은 변경하지 않았습니다. 마지막 곡만 별도 키에 저장하며 재생 위치·자동 재생 상태는 저장하지 않습니다. MP3/LRC는 앱 설치·업데이트 사전 캐시에서 제외하고 필요할 때 요청합니다. Range 요청은 브라우저가 직접 처리합니다. 음악 재생이나 가사/곡 선택 창이 열려 있는 동안 자동 업데이트를 보류하며, 일시정지하고 창을 닫으면 기존 안전 업데이트가 이어집니다.

## 검증 범위

로컬 Chrome 및 별도 Chrome Headless Shell에서 실제 제공 MP3의 재생/일시정지, 진행바와 가사 선택 이동, 다음 곡 재생, 마지막 곡 종료, 재접속 자동 재생 금지, MP3/LRC 404, 기록 원문 보존을 확인했습니다. 320/390/1280px의 라이트·다크 화면을 검사했고 가로 넘침이 없습니다. 서비스워커 캐시에 MP3/LRC가 없으며 실제 새 워커를 내려받아 재생 중 대기 → 일시정지 후 갱신 → 기록 보존도 확인했습니다. 기존 달력·저장·설치·오프라인을 포함한 **단위 테스트 80개, 브라우저 테스트 42개 모두 통과**했습니다.

PC에 설치된 IDM이 Chrome의 일부 MP3 요청을 `204 Intercepted by the IDM Advanced Integration`으로 가로채는 환경 문제가 확인되어, 최종 브라우저 검증은 프로젝트 안의 별도 Headless Shell을 사용했습니다. 사용자 PC의 프로그램이나 브라우저 설정은 바꾸지 않았습니다. 기존 테스트의 비동기 저장은 완료 화면을 확인한 뒤 검증하도록 보완했습니다.

Android Chrome / Samsung Internet / iPhone의 실제 기기 재생, 블루투스, 잠금 화면·백그라운드 재생은 **미검증**입니다. Android 실제 설치창과 홈 화면 실행의 실기기 검증 상태는 `INSTALLATION_REPORT.md`를 따릅니다.

공개 앱 주소: https://sojin7814.github.io/dragon/
